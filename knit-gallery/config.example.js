// このファイルを `config.js` にリネームして、中身を Firebase の設定値で埋めてください。
// `apiKey` などは公開しても問題ない値です。秘密鍵などは置かないでください。
//
// 例:
// - Firebase コンソール -> プロジェクト設定 -> 全般 -> Your apps の設定
//
// `window.FIREBASE_CONFIG` に設定します（このサイト内のコードが参照）。

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};

